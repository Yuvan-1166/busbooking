import { API_BASE } from "../../api";

export default function Footer() {
  return (
    <footer className="mx-auto flex max-w-[1168px] justify-between border-t border-line px-0 py-6 font-mono text-[10px] text-muted">
      <span>Bus Booking</span>
    </footer>
  );
}
